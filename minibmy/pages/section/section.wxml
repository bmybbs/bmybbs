<view>
	<view class="container3">
		<view class="section" wx:for="{{list}}" wx:for-index="idx" wx:for-item="item" wx:key="id">
			<text class="section_id">{{item.id}}</text>
			<text class="section_name">{{item.text}}</text>
		</view>
	</view>
</view>

